{{#if isOpen}}
  {{set-body-class 'mobile-menu--open'}}
{{/if}}

{{mobile-menu/mask
  isOpen=isOpen
  position=position
  onClick=(action 'close')
}}

{{#mobile-menu/tray
  width=width
  isLeft=isLeft
  position=position
  onClose=(action 'close')
}}
  {{yield (hash
    link-to=(component 'mobile-menu/link-to' onClick=(action 'close'))
  )}}
{{/mobile-menu/tray}}